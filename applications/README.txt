All projects under applications dir are not code that can run. Instead
These projects are source code that we use to run our static analysis.
Folders containing "Double-size" or "Triple-size" are the same code
as the one without these two signs but with different sizes.